# Core runtime dependencies for training, model import, and loader execution.
#
# If you need a CUDA-specific PyTorch build, install `torch` and `torchvision`
# using the command recommended at https://pytorch.org/get-started/locally/
# for your system, then install the remaining packages from this file.

torch
torchvision
numpy
opencv-python-headless
Pillow
PyYAML
tensorboard
tensorboardX
tqdm
einops
matplotlib
